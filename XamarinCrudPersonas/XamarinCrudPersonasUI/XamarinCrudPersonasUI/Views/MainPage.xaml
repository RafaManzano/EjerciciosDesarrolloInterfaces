<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XamarinCrudPersonasUI"
             x:Class="XamarinCrudPersonasUI.MainPage"
             xmlns:vm="clr-namespace:XamarinCrudPersonasUI.ModelViews">

    <ContentPage.BindingContext>
        <vm:MainPageVM></vm:MainPageVM>
    </ContentPage.BindingContext>

    <StackLayout>
        <ListView ItemsSource="{Binding Personas}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Path=Nombre}"></Label>
                            <Label Text="{Binding Path=Apellidos}"></Label>
                        </StackLayout>
                    </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
         </ListView>
        <StackLayout Orientation="Horizontal">
            <Button x:Name="anhadir" Text="Anhadir"></Button>
            <Button x:Name="borrar" Text="Borrar"></Button>
        </StackLayout>
     </StackLayout>

</ContentPage>
